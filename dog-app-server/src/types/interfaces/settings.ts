export const Settings = {
	apiVersion: process.env.API_VERSION,
	port: process.env.PORT,
	defaultPaginationSize: process.env.DEFAULT_PAGINATION_SIZE,
	cacheExpiresAfterMinutes: process.env.CACHE_EXPIRES_AFTER_MINUTES
}
